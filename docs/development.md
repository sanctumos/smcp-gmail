# Development (obsolete stub)

```bash
pip install -r requirements-testing.txt
python3 run_tests.py
```

Coverage modules: `cli`, `auth_config`, `imap_ops`, `smtp_ops`, and helpers. See `run_tests.py` for `cov-fail-under`.
